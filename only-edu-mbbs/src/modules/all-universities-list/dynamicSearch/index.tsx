import { UniversitiesData } from "@/types/types";
import React from "react";

interface Props {
  data: UniversitiesData;
}

const DynamicSearchComponent = ({ data }: Props) => {
  return <></>;
};

export default DynamicSearchComponent;
